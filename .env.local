# LiteLLM proxy config used by the frontend OpenAI client
# Base URL is hardcoded to https://asgard.bhishmaraj.org in services/geminiService.ts

# LiteLLM proxy API key (master or virtual key)
VITE_LITELLM_API_KEY="sk-nECDN21H3yfEAbuIrEnU3A"

# Model alias routed by LiteLLM (Gemini 2.5 Flash)
VITE_LLM_MODEL="gemini-2.5-flash"
