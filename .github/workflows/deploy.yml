# GitHub Actions workflow for deploying Colyseus to Google Cloud Run
# Next.js stays on Vercel (progressive migration approach)
# Each branch gets its own Colyseus deployment (e.g., feat/xyz -> simulacra-colyseus-feat-xyz)
# Main branch deploys to production with custom domain (game.simulacra.cc)

name: Deploy Colyseus to Cloud Run

on:
  push:
    branches:
      - main
      - 'feat/*'
      - 'fix/*'
      - 'chore/*'

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: 'read'
      id-token: 'write'

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Google Auth
        id: auth
        uses: google-github-actions/auth@v2
        with:
          credentials_json: '${{ secrets.GCP_CREDENTIALS }}'

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Submit Build
        run: |
          gcloud builds submit --config cloudbuild.yaml \
            --substitutions=BRANCH_NAME=${GITHUB_REF_NAME},COMMIT_SHA=${GITHUB_SHA} \
            .
