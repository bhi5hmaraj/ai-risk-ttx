# Grafana datasource provisioning
# Auto-configures Loki as a datasource when Grafana starts

apiVersion: 1

datasources:
  - name: Loki
    type: loki
    access: proxy
    url: http://loki:3100
    isDefault: true
    jsonData:
      maxLines: 1000
    editable: true
